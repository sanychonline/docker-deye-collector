prometheus.scrape "deye" {
  targets = [
    {
      "__address__" = "localhost:9101",
    },
  ]

  metrics_path    = "/deye.prom"
  scrape_interval = "60s"

  forward_to = [prometheus.remote_write.grafanacloud.receiver]
}

prometheus.scrape "trader_containers" {
  targets = [
    {
      "__address__" = "trader-bot-cadvisor:8080",
    },
  ]

  metrics_path    = "/metrics"
  scrape_interval = "30s"

  forward_to = [prometheus.remote_write.grafanacloud.receiver]
}

prometheus.remote_write "grafanacloud" {
  endpoint {
    url = env("GRAFANA_URL")

    basic_auth {
      username = env("GRAFANA_USER")
      password = env("GRAFANA_API_KEY")
    }
  }
}
